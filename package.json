{
  "devDependencies": {
    "@testing-library/react": "^16.1.0"
  },
  "scripts": {
    "build": "echo VERSION=$npm_package_version && npm run build",
    "test": "echo 'Running tests'",
    "build-and-test": "npm run build && npm run test",
    "build-and-test-parallel": "npm run build & npm run test"
  }
}
